package chapterSixteenMedium

/**
 * Just takes O n^2 possible lines between two points
 * Then put in inside the hashtable, be careful of rounding because we are using double
 * then for each slope > get a list of lines > for each line > countEquivalent with the entire hash table
 *
 * return the best line
 */